// This file is auto-generated by @hey-api/openapi-ts

import { client } from './client.gen.js';
import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client/index.js';
import type { CancelOrderData, CancelOrderErrors, CancelOrderResponses, CreateData, CreateErrors, CreateResponses, DeleteData, DeleteErrors, DeleteResponses, DividendsData, DividendsErrors, DividendsResponses, DuplicatePieData, DuplicatePieErrors, DuplicatePieResponses, ExchangesData, ExchangesErrors, ExchangesResponses, GetAccountSummaryData, GetAccountSummaryErrors, GetAccountSummaryResponses, GetAllData, GetAllErrors, GetAllResponses, GetDetailedData, GetDetailedErrors, GetDetailedResponses, GetPositionsData, GetPositionsErrors, GetPositionsResponses, GetReportsData, GetReportsErrors, GetReportsResponses, InstrumentsData, InstrumentsErrors, InstrumentsResponses, OrderByIdData, OrderByIdErrors, OrderByIdResponses, Orders1Data, Orders1Errors, Orders1Responses, OrdersData, OrdersErrors, OrdersResponses, PlaceLimitOrderData, PlaceLimitOrderErrors, PlaceLimitOrderResponses, PlaceMarketOrderData, PlaceMarketOrderErrors, PlaceMarketOrderResponses, PlaceStopOrder1Data, PlaceStopOrder1Errors, PlaceStopOrder1Responses, PlaceStopOrderData, PlaceStopOrderErrors, PlaceStopOrderResponses, RequestReportData, RequestReportErrors, RequestReportResponses, TransactionsData, TransactionsErrors, TransactionsResponses, UpdateData, UpdateErrors, UpdateResponses } from './types.gen.js';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Get account summary
 *
 * Provides a breakdown of your account's cash and investment metrics,
 * including available funds, invested capital, and total account value.
 *
 * **Rate limit:** 1 req / 5s
 */
export const getAccountSummary = <ThrowOnError extends boolean = false>(options?: Options<GetAccountSummaryData, ThrowOnError>): RequestResult<GetAccountSummaryResponses, GetAccountSummaryErrors, ThrowOnError> => (options?.client ?? client).get<GetAccountSummaryResponses, GetAccountSummaryErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/account/summary',
    ...options
});

/**
 * Get paid out dividends
 *
 *
 *
 * **Rate limit:** 6 req / 1m0s
 */
export const dividends = <ThrowOnError extends boolean = false>(options?: Options<DividendsData, ThrowOnError>): RequestResult<DividendsResponses, DividendsErrors, ThrowOnError> => (options?.client ?? client).get<DividendsResponses, DividendsErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/history/dividends',
    ...options
});

/**
 * List generated reports
 *
 * Retrieves a list of all requested CSV reports and their current status.
 *
 *
 * **Asynchronous Workflow:**
 *
 * 1. Call `POST /history/exports` to request a report. You will receive a
 * `reportId`.
 *
 * 2. Periodically call this endpoint (`GET /history/exports`) to check the
 * `status` of the report corresponding to your `reportId`.
 *
 * 3. Once the status is `Finished`, the `downloadLink` field will contain
 * a URL to download the CSV file.
 *
 * **Rate limit:** 1 req / 1m0s
 */
export const getReports = <ThrowOnError extends boolean = false>(options?: Options<GetReportsData, ThrowOnError>): RequestResult<GetReportsResponses, GetReportsErrors, ThrowOnError> => (options?.client ?? client).get<GetReportsResponses, GetReportsErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/history/exports',
    ...options
});

/**
 * Request a CSV report
 *
 * Initiates the generation of a CSV report containing historical account
 * data. This is an asynchronous operation. The response will include a
 * `reportId` which you can use to track the status of the generation
 * process using the `GET /history/exports` endpoint.
 *
 * **Rate limit:** 1 req / 30s
 */
export const requestReport = <ThrowOnError extends boolean = false>(options: Options<RequestReportData, ThrowOnError>): RequestResult<RequestReportResponses, RequestReportErrors, ThrowOnError> => (options.client ?? client).post<RequestReportResponses, RequestReportErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/history/exports',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get historical orders data
 *
 *
 *
 * **Rate limit:** 6 req / 1m0s
 */
export const orders1 = <ThrowOnError extends boolean = false>(options?: Options<Orders1Data, ThrowOnError>): RequestResult<Orders1Responses, Orders1Errors, ThrowOnError> => (options?.client ?? client).get<Orders1Responses, Orders1Errors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/history/orders',
    ...options
});

/**
 * Get transactions
 *
 * Fetch superficial information about movements to and from your account
 *
 * **Rate limit:** 6 req / 1m0s
 */
export const transactions = <ThrowOnError extends boolean = false>(options?: Options<TransactionsData, ThrowOnError>): RequestResult<TransactionsResponses, TransactionsErrors, ThrowOnError> => (options?.client ?? client).get<TransactionsResponses, TransactionsErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/history/transactions',
    ...options
});

/**
 * Get exchanges metadata
 *
 * Retrieves all accessible exchanges and their corresponding working schedules.
 * Data is refreshed every 10 minutes.
 *
 * **Rate limit:** 1 req / 30s
 */
export const exchanges = <ThrowOnError extends boolean = false>(options?: Options<ExchangesData, ThrowOnError>): RequestResult<ExchangesResponses, ExchangesErrors, ThrowOnError> => (options?.client ?? client).get<ExchangesResponses, ExchangesErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/metadata/exchanges',
    ...options
});

/**
 * Get all available instruments
 *
 * Retrieves all accessible instruments.
 * Data is refreshed every 10 minutes.
 *
 * **Rate limit:** 1 req / 50s
 */
export const instruments = <ThrowOnError extends boolean = false>(options?: Options<InstrumentsData, ThrowOnError>): RequestResult<InstrumentsResponses, InstrumentsErrors, ThrowOnError> => (options?.client ?? client).get<InstrumentsResponses, InstrumentsErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/metadata/instruments',
    ...options
});

/**
 * Get all pending orders
 *
 * Retrieves a list of all orders that are currently active (i.e., not yet
 * filled, cancelled, or expired). This is useful for monitoring the status
 * of your open positions and managing your trading strategy.
 *
 * **Rate limit:** 1 req / 5s
 */
export const orders = <ThrowOnError extends boolean = false>(options?: Options<OrdersData, ThrowOnError>): RequestResult<OrdersResponses, OrdersErrors, ThrowOnError> => (options?.client ?? client).get<OrdersResponses, OrdersErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders',
    ...options
});

/**
 * Place a Limit order
 *
 * Creates a new Limit order, which executes at a specified price or
 * better.
 *
 * - To place a **buy** order, use a positive `quantity`. The order will
 * fill at the `limitPrice` or lower.
 *
 * - To place a **sell** order, use a negative `quantity`. The order will
 * fill at the `limitPrice` or higher.
 *
 *
 *
 * **Order Limitations**
 *
 * * Orders can be executed only in the **main account currency**
 *
 *
 * **Important:** In this beta version, this endpoint is **not
 * idempotent**. Sending the same request multiple times may result in
 * duplicate orders.
 *
 * **Rate limit:** 1 req / 2s
 */
export const placeLimitOrder = <ThrowOnError extends boolean = false>(options: Options<PlaceLimitOrderData, ThrowOnError>): RequestResult<PlaceLimitOrderResponses, PlaceLimitOrderErrors, ThrowOnError> => (options.client ?? client).post<PlaceLimitOrderResponses, PlaceLimitOrderErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/limit',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Place a Market order
 *
 * Creates a new Market order, which is an instruction to trade a security
 * immediately at the next available price.
 *
 * - To place a **buy** order, use a positive `quantity`.
 *
 * - To place a **sell** order, use a negative `quantity`.
 *
 *
 * - **`extendedHours`**: Set to `true` to allow the order to be filled
 * outside of the standard trading session.
 *
 * - If placed when the market is closed, the order will be queued to
 * execute when the market next opens.
 *
 * x
 * **Order Limitations**
 *
 * * Orders can be executed only in the **main account currency**
 *
 *
 * **Warning:** Market orders can be subject to price slippage, where the
 * final execution price may differ from the price at the time of order
 * placement.
 *
 *
 * **Important:** In this beta version, this endpoint is **not
 * idempotent**. Sending the same request multiple times may result in
 * duplicate orders.
 *
 * **Rate limit:** 50 req / 1m0s
 */
export const placeMarketOrder = <ThrowOnError extends boolean = false>(options: Options<PlaceMarketOrderData, ThrowOnError>): RequestResult<PlaceMarketOrderResponses, PlaceMarketOrderErrors, ThrowOnError> => (options.client ?? client).post<PlaceMarketOrderResponses, PlaceMarketOrderErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/market',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Place a Stop order
 *
 * Creates a new Stop order, which places a Market order once the
 * `stopPrice` is reached.
 *
 * - To place a **buy** stop order, use a positive `quantity`.
 *
 * - To place a **sell** stop order (commonly a 'stop-loss'), use a
 * negative `quantity`.
 *
 *
 * - The `stopPrice` is triggered by the instrument's **Last Traded Price
 * (LTP)**.
 *
 *
 *
 *
 * **Order Limitations**
 *
 * * Orders can be executed only in the **main account currency**
 *
 * **Important:** In this beta version, this endpoint is **not
 * idempotent**. Sending the same request multiple times may result in
 * duplicate orders.
 *
 * **Rate limit:** 1 req / 2s
 */
export const placeStopOrder1 = <ThrowOnError extends boolean = false>(options: Options<PlaceStopOrder1Data, ThrowOnError>): RequestResult<PlaceStopOrder1Responses, PlaceStopOrder1Errors, ThrowOnError> => (options.client ?? client).post<PlaceStopOrder1Responses, PlaceStopOrder1Errors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/stop',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Place a StopLimit order
 *
 * Creates a new Stop-Limit order, combining features of a Stop and a Limit
 * order. The direction of the trade (buy/sell) is determined by the sign
 * of the `quantity` field.
 *
 *
 * **Execution Logic:**
 *
 * 1.  When the instrument's **Last Traded Price (LTP)** reaches the
 * specified `stopPrice`, the order is triggered.
 *
 * 2.  A Limit order is then automatically placed at the specified
 * `limitPrice`.
 *
 *
 * This two-step process helps protect against price slippage that can
 * occur with a standard Stop order.
 *
 *
 * **Order Limitations**
 *
 * * Orders can be executed only in the **main account currency**
 *
 *
 * **Important:** In this beta version, this endpoint is **not
 * idempotent**. Sending the same request multiple times may result in
 * duplicate orders.
 *
 * **Rate limit:** 1 req / 2s
 */
export const placeStopOrder = <ThrowOnError extends boolean = false>(options: Options<PlaceStopOrderData, ThrowOnError>): RequestResult<PlaceStopOrderResponses, PlaceStopOrderErrors, ThrowOnError> => (options.client ?? client).post<PlaceStopOrderResponses, PlaceStopOrderErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/stop_limit',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a pending order
 *
 * Attempts to cancel an active, unfilled order by its unique ID.
 * Cancellation is not guaranteed if the order is already in the process of
 * being filled. A successful response indicates the cancellation request
 * was accepted.
 *
 * **Rate limit:** 50 req / 1m0s
 */
export const cancelOrder = <ThrowOnError extends boolean = false>(options: Options<CancelOrderData, ThrowOnError>): RequestResult<CancelOrderResponses, CancelOrderErrors, ThrowOnError> => (options.client ?? client).delete<CancelOrderResponses, CancelOrderErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/{id}',
    ...options
});

/**
 * Get a pending order by ID
 *
 * Retrieves a single pending order using its unique numerical ID. This is
 * useful for checking the status of a specific order you have previously
 * placed.
 *
 * **Rate limit:** 1 req / 1s
 */
export const orderById = <ThrowOnError extends boolean = false>(options: Options<OrderByIdData, ThrowOnError>): RequestResult<OrderByIdResponses, OrderByIdErrors, ThrowOnError> => (options.client ?? client).get<OrderByIdResponses, OrderByIdErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/orders/{id}',
    ...options
});

/**
 * Fetch all pies
 *
 * Fetches all pies for the account
 *
 * **Rate limit:** 1 req / 30s
 *
 * @deprecated
 */
export const getAll = <ThrowOnError extends boolean = false>(options?: Options<GetAllData, ThrowOnError>): RequestResult<GetAllResponses, GetAllErrors, ThrowOnError> => (options?.client ?? client).get<GetAllResponses, GetAllErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies',
    ...options
});

/**
 * Create pie
 *
 * Creates a pie for the account by given params
 *
 * **Rate limit:** 1 req / 5s
 *
 * @deprecated
 */
export const create = <ThrowOnError extends boolean = false>(options: Options<CreateData, ThrowOnError>): RequestResult<CreateResponses, CreateErrors, ThrowOnError> => (options.client ?? client).post<CreateResponses, CreateErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete pie
 *
 * Deletes a pie by given id
 *
 * **Rate limit:** 1 req / 5s
 *
 * @deprecated
 */
export const delete_ = <ThrowOnError extends boolean = false>(options: Options<DeleteData, ThrowOnError>): RequestResult<DeleteResponses, DeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteResponses, DeleteErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies/{id}',
    ...options
});

/**
 * Fetch a pie
 *
 * Fetches a pies for the account with detailed information
 *
 * **Rate limit:** 1 req / 5s
 *
 * @deprecated
 */
export const getDetailed = <ThrowOnError extends boolean = false>(options: Options<GetDetailedData, ThrowOnError>): RequestResult<GetDetailedResponses, GetDetailedErrors, ThrowOnError> => (options.client ?? client).get<GetDetailedResponses, GetDetailedErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies/{id}',
    ...options
});

/**
 * Update pie
 *
 * Updates a pie for the account by given params
 *
 * **Rate limit:** 1 req / 5s
 *
 * @deprecated
 */
export const update = <ThrowOnError extends boolean = false>(options: Options<UpdateData, ThrowOnError>): RequestResult<UpdateResponses, UpdateErrors, ThrowOnError> => (options.client ?? client).post<UpdateResponses, UpdateErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Duplicate pie
 *
 * Duplicates a pie for the account
 *
 * **Rate limit:** 1 req / 5s
 *
 * @deprecated
 */
export const duplicatePie = <ThrowOnError extends boolean = false>(options: Options<DuplicatePieData, ThrowOnError>): RequestResult<DuplicatePieResponses, DuplicatePieErrors, ThrowOnError> => (options.client ?? client).post<DuplicatePieResponses, DuplicatePieErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/pies/{id}/duplicate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetch all open positions
 *
 * Fetch all open positions for your account
 *
 * **Rate limit:** 1 req / 1s
 */
export const getPositions = <ThrowOnError extends boolean = false>(options?: Options<GetPositionsData, ThrowOnError>): RequestResult<GetPositionsResponses, GetPositionsErrors, ThrowOnError> => (options?.client ?? client).get<GetPositionsResponses, GetPositionsErrors, ThrowOnError>({
    security: [{ scheme: 'basic', type: 'http' }],
    url: '/api/v0/equity/positions',
    ...options
});
